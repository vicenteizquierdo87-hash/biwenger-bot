version: '3.8'

services:
  biwenger-bot:
    build: .
    container_name: biwenger-bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
